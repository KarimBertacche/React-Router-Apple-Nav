import React from 'react';
import { NavLink } from 'react-router-dom';

export default function NavWrapper() {
    return(
        <header>
            <NavLink>
                

            </NavLink> 
        </header>
    );
}